import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#0ABB87',
    },
    background: {
      default: '#f9fbff',
    },
    grey: {
      50: '#EEF0F5',
    },
  },
})

export default theme
